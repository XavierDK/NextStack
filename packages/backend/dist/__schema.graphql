"""
THIS FILE IS GENERATED
DO NOT EDIT IT!!!
Create your schema inside the ./schemas directory instead and run npm run schema:generate
"""


type Task {
  id: ID!
  title: String!
  completed: Boolean!
  user: User!
}

type TaskCount {
  number: Int
}

type Query {
  taskCountForUser(id: ID!): TaskCount @custom(dql: """
     query q($id: string){
       taskCountForUser(func: uid($id), first: 1) {
           number: count(User.tasks)
       }
    }
  """)
}

type User {
  sub: String! @id
  email: String!
  name: String! @search(by: [term])
  tasks: [Task!] @hasInverse(field: user)
  hello: String! @lambda
}

schema {
  query: Query
}
