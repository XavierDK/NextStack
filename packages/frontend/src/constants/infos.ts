type Infos = {
  siteName: string;
  siteUrl: string;
  description: string;
  companyName: string;
};

const infos: Infos = {
  siteName: 'NextStack',
  siteUrl: 'http://localhost:3000',
  description: 'NextStack Template',
  companyName: 'NextStack'
};

export default infos;
