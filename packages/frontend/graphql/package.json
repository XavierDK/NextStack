{
  "name": "graphql",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "build": "webpack --target=webworker",
    "dev": "webpack --target=webworker --watch",
    "schema": "curl -X POST localhost:8080/admin/schema --data-binary @schema.graphql",
    "schema:watch": "chokidar 'schema.graphql' -c 'curl -X POST localhost:8080/admin/schema --data-binary @schema.graphql'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "description": "",
  "devDependencies": {
    "@slash-graphql/lambda-types": "^1.2.0",
    "chokidar-cli": "^2.1.0",
    "ts-loader": "^8.0.17",
    "typescript": "^4.2.3",
    "webpack": "^5.24.3",
    "webpack-cli": "^4.5.0"
  }
}
